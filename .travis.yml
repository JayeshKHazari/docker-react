sudo: required
services: 
  - docker

before_install:
  - gem install bundler --pre
  - docker build -t jayeshkhazari/docker-react -f Dockerfile.dev .

scripts:
  - docker run jayeshkhazari/docker-react npm run test -- --coverage
